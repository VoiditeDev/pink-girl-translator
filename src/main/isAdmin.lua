return function(player: Player)
	-- This must be changed as it checks if the player in my group's rank is higher than one, you should change it to your group or whatever
	return player --player:GetRankInGroup(13934532) > 1
end
